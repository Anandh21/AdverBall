{% extends 'base.html.twig' %}

{% block title %}Family{% endblock %}

{% block body %}
    <h1>Family</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ family.id }}</td>
            </tr>
            <tr>
                <th>NumberMember</th>
                <td>{{ family.numberMember }}</td>
            </tr>
            <tr>
                <th>Qtl</th>
                <td>{{ family.qtl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('family_index') }}">back to list</a>

    <a href="{{ path('family_edit', {'id': family.id}) }}">edit</a>

    {{ include('family/_delete_form.html.twig') }}
{% endblock %}
