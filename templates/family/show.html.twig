{% extends 'base.html.twig' %}

{% block title %}Family{% endblock %}

{% block body %}
    <h1>Famille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ family.id }}</td>
            </tr>
            <tr>
                <th>NombreMembre</th>
                <td>{{ family.numberMember }}</td>
            </tr>
            <tr>
                <th>Qtl</th>
                <td>{{ family.qtl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('family_index') }}">Retour à la liste</a>

    <a href="{{ path('family_edit', {'id': family.id}) }}">Modifier</a>

    {{ include('family/_delete_form.html.twig') }}
{% endblock %}
